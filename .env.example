# Qwen Code 내부망 LLM 설정 예시
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# ===========================================
# 방법 1: 내부망 LLM 서버 직접 연결 (우선순위 1)
# ===========================================
LLM_BASE_URL=https://10.0.0.2:8443/v1/chat/completions
# LLM_API_KEY=your-internal-llm-api-key
# LLM_MODEL=your-internal-model-name

# ===========================================
# 방법 2: 프록시 서버를 통한 연결 (우선순위 2)
# ===========================================
# PROXY_SERVER_URL=http://localhost:8443/devport/api/v1/chat/completions
# OPENROUTER_API_KEY=your-openrouter-api-key-here

# ===========================================
# SSL 설정
# ===========================================
# SSL 인증서 검증 우회 해제 (기본적으로 우회됨)
# DISABLE_SSL_SKIP=true

# ===========================================
# 하위호환성 (기존 설정도 지원)
# ===========================================
# INTERNAL_LLM_BASE_URL=http://localhost:8443/devport/api/v1
# INTERNAL_LLM_API_KEY=test-key
# INTERNAL_LLM_MODEL=internal-llm-model

# ===========================================
# 사용 안내
# ===========================================
# 1. LLM_BASE_URL이 설정되면 내부망 서버로 직접 연결
# 2. LLM_BASE_URL이 없고 PROXY_SERVER_URL이 있으면 프록시로 연결  
# 3. 둘 다 없으면 기본값(localhost:8443) 사용